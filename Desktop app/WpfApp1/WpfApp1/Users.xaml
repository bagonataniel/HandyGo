<UserControl x:Class="WpfApp1.Users"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <SolidColorBrush x:Key="tableBg" Color="#1e293b"/>
        <SolidColorBrush x:Key="darkBg" Color="#0f172a"/>
        <SolidColorBrush x:Key="Border" Color="#354458"/>
        <SolidColorBrush x:Key="primaryText" Color="#ffffff"/>
        <SolidColorBrush x:Key="secondaryText" Color="#7f97b0"/>
        <SolidColorBrush x:Key="pending" Color="#f69e0b"/>
        <SolidColorBrush x:Key="pendingBorder" Color="#f69e0b" Opacity="1"/>
        <SolidColorBrush x:Key="pendingBg" Color="#f69e0b" Opacity="0.5"/>
        <SolidColorBrush x:Key="approved" Color="#10b981"/>
        <SolidColorBrush x:Key="approvedBorder" Color="#10b981" Opacity="1"/>
        <SolidColorBrush x:Key="approvedBg" Color="#10b981" Opacity="0.5"/>
        <SolidColorBrush x:Key="rejected" Color="#f43f5e"/>
        <SolidColorBrush x:Key="rejectedBorder" Color="#f43f5e" Opacity="1"/>
        <SolidColorBrush x:Key="rejectedBg" Color="#f43f5e" Opacity="0.5"/>
        <Style x:Key="NoCircleRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource secondaryText}" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                        Background="{StaticResource darkBg}"
                        CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1e293b" />
                                <Setter Property="Foreground" Value="#137fec"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2f3847" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="7.5*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Grid.Row="0">
            <Label Foreground="{StaticResource secondaryText}" FontSize="15" Margin="20 10 0 0">Kezdőlap / Felhasználók</Label>
            <Label Foreground="#F0F0F0" FontSize="25" FontWeight="SemiBold" Margin="20 0 0 0">Felhasználók listája</Label>
        </StackPanel>

        <Border Background="#1e293b" Grid.Row="1" Margin="20 0" Padding="10" CornerRadius="15" BorderThickness="1" BorderBrush="#354458">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6*"></ColumnDefinition>
                    <ColumnDefinition Width="4*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Border CornerRadius="5" Grid.Column="0" Background="{StaticResource darkBg}" Padding="10" VerticalAlignment="Center">
                    <TextBox BorderThickness="0" FontSize="15" Background="Transparent" Foreground="{StaticResource primaryText}" x:Name="searchbox" TextChanged="searchbox_TextChanged"></TextBox>
                </Border>

                <Border Background="{StaticResource darkBg}" Grid.Column="1" Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" CornerRadius="5">
                    <UniformGrid Rows="1" Columns="3" VerticalAlignment="Center" Margin="10 0">
                        <RadioButton x:Name="allFilter" GroupName="filter" Style="{StaticResource NoCircleRadioButton}" Click="filterClick" Content="Összes"/>
                        <RadioButton x:Name="verifiedFilter" GroupName="filter" Style="{StaticResource NoCircleRadioButton}" Click="filterClick" Content="Hitelesített"/>
                        <RadioButton x:Name="notverifiedFilter" GroupName="filter" Style="{StaticResource NoCircleRadioButton}" Click="filterClick" Content="Nem hitelesített"/>
                    </UniformGrid>
                </Border>
            </Grid>
        </Border>

        <Border Background="#1e293b" Grid.Row="2" Margin="20" CornerRadius="15" BorderThickness="1" BorderBrush="#354458">
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" MaxHeight="50"></RowDefinition>
                    <RowDefinition Height="7.5*"></RowDefinition>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" BorderBrush="{StaticResource Border}" BorderThickness="0 0 0 1">
                    <Grid x:Name="tableFieldContainer">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.5*" />
                            <ColumnDefinition Width="1.2*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="0.5*" />
                            <ColumnDefinition Width="0.8*" />
                        </Grid.ColumnDefinitions>

                        <Label VerticalAlignment="Center" FontSize="15" Grid.Column="0" FontWeight="Bold" Foreground="{StaticResource secondaryText}">NÉV</Label>
                        <Label VerticalAlignment="Center" FontSize="15" Grid.Column="1" FontWeight="Bold" Foreground="{StaticResource secondaryText}">BIÓ</Label>
                        <Label VerticalAlignment="Center" FontSize="15" Grid.Column="2" FontWeight="Bold" Foreground="{StaticResource secondaryText}">KÉSZSÉGEK</Label>
                        <Label VerticalAlignment="Center" FontSize="15" Grid.Column="3" FontWeight="Bold" Foreground="{StaticResource secondaryText}">HELY</Label>
                        <Label VerticalAlignment="Center" FontSize="15" Grid.Column="4" FontWeight="Bold" Foreground="{StaticResource secondaryText}">HITELESÍTETT</Label>
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <ItemsControl ItemsSource="{Binding UsersData}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Height="70"
                        HorizontalAlignment="Stretch"
                        BorderThickness="0 1 0 1"
                        BorderBrush="#354458">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1.5*" />
                                            <ColumnDefinition Width="1.2*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="0.5*" />
                                            <ColumnDefinition Width="0.8*" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <Label Foreground="{StaticResource primaryText}"
                                   Content="{Binding name}" FontSize="17" FontWeight="SemiBold"/>
                                            <Label Foreground="{StaticResource secondaryText}"
                                   Content="{Binding email, StringFormat=email: {0}}" />
                                        </StackPanel>

                                        <Label Grid.Column="1"
                               VerticalAlignment="Center"
                               Foreground="{StaticResource primaryText}"
                               FontSize="15"
                               Content="{Binding bio}" />

                                        <Label Grid.Column="2"
                               VerticalAlignment="Center"
                               FontSize="15"
                               Foreground="{StaticResource primaryText}"
                               Content="{Binding skills}" />

                                        <StackPanel Grid.Column="3" VerticalAlignment="Center">
                                            <Label Foreground="{StaticResource secondaryText}"
                                   Content="{Binding location}">
                                                <Label.ToolTip>
                                                    <ToolTip Background="#1e293b" BorderBrush="#354458" BorderThickness="1">
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding latitude}" FontWeight="Bold" Foreground="White"/>
                                                            <TextBlock Text="{Binding longitude}" Foreground="#7f97b0"/>
                                                        </StackPanel>
                                                    </ToolTip>
                                                </Label.ToolTip>
                                            </Label>
                                        </StackPanel>

                                        <StackPanel Grid.Column="4" HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal" Margin="0 0 20 0">
                                            <Path Data="M480-280q83 0 141.5-58.5T680-480q0-83-58.5-141.5T480-680q-83 0-141.5 58.5T280-480q0 83 58.5 141.5T480-280Zm0 200q-83 0-156-31.5T197-197q-54-54-85.5-127T80-480q0-83 31.5-156T197-763q54-54 127-85.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 83-31.5 156T763-197q-54 54-127 85.5T480-80Zm0-80q134 0 227-93t93-227q0-134-93-227t-227-93q-134 0-227 93t-93 227q0 134 93 227t227 93Z" Width="30" Height="20" Stretch="Uniform">
                                                <Path.Style>
                                                    <Style TargetType="Path">
                                                        <Setter Property="Fill" Value="{StaticResource rejected}"/>

                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding is_verified}" Value="0">
                                                                <Setter Property="Fill" Value="{StaticResource rejected}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding is_verified}" Value="1">
                                                                <Setter Property="Fill" Value="{StaticResource approved}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Path.Style>
                                            </Path>
                                            <Label Foreground="{StaticResource primaryText}" FontSize="15" Content="{Binding is_verified_text}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
