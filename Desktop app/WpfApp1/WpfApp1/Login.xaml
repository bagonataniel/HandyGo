<Window x:Class="WpfApp1.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="loginWindow" Height="250" Width="560">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username"/>
        <TextBox Grid.Row="1" x:Name="username" Text="bagonataniel" />

        <TextBlock Grid.Row="2" Text="Password" Margin="0,10,0,0"/>
        <PasswordBox Grid.Row="3"
                     x:Name="password"/>

        <Button Content="Login" Grid.Row="4" Margin="0,15,0,0" Height="35" Click="Button_Click" HorizontalAlignment="Stretch"/>
    </Grid>
</Window>
