<UserControl x:Class="WpfApp1.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <UserControl.Resources>
        <SolidColorBrush x:Key="Text" Color="#eff0f2"/>
        <SolidColorBrush x:Key="TextLight0" Color="#d3d4d6"/>
        <SolidColorBrush x:Key="TextLight1" Color="#6a7e96"/>
        <DropShadowEffect x:Key="boxShadow"
                      Color="Black"
                      Direction="320"
                      ShadowDepth="5"
                      BlurRadius="10"
                      Opacity="0.5"/>
        <Style x:Key="FocusBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#25394c"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsKeyboardFocusWithin, RelativeSource={RelativeSource Self}}" Value="True">
                    <Setter Property="BorderBrush" Value="#4e5861"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="NoHoverButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>

        <Grid.Background>
            <LinearGradientBrush EndPoint="1,1" StartPoint="0,0">
                <GradientStop Color="#FF102235"/>
                <GradientStop Color="#FF0D1B2B" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="2*" MaxWidth="500"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="6*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Border Background="#1e293b" Grid.Column="1" Grid.Row="1" CornerRadius="20" Effect="{StaticResource boxShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="9*"></RowDefinition>
                    <RowDefinition Height="1*"></RowDefinition>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Vertical" Grid.Row="0" Margin="20">
                    <Border Background="#1c314d" Width="50" Height="50" CornerRadius="10" Margin="0 20 0 10" Padding="3">
                        <Path Data="M420-360h120l-23-129q20-10 31.5-29t11.5-42q0-33-23.5-56.5T480-640q-33 0-56.5 23.5T400-560q0 23 11.5 42t31.5 29l-23 129Zm60 280q-139-35-229.5-159.5T160-516v-244l320-120 320 120v244q0 152-90.5 276.5T480-80Zm0-84q104-33 172-132t68-220v-189l-240-90-240 90v189q0 121 68 220t172 132Zm0-316Z" Fill="#137fec" Stretch="Uniform" />
                    </Border>
                    <Label HorizontalAlignment="Center" Foreground="{StaticResource Text}" FontSize="30" FontWeight="Bold">HandyGo Admin</Label>
                    <Label HorizontalAlignment="Center" Foreground="{StaticResource TextLight1}" Margin="0 0 0 20" FontSize="17">Üdv újra! A folytatáshoz jelentkezz be!</Label>
                    <Label FontSize="20" Foreground="{StaticResource TextLight0}">Felhasználónév</Label>
                    <Border Style="{StaticResource FocusBorderStyle}" Background="#192633" CornerRadius="5" BorderThickness="1">
                        <TextBox Margin="5" Foreground="{StaticResource TextLight1}" Background="Transparent" BorderThickness="0" FontSize="25" x:Name="username"></TextBox>
                    </Border>
                    <Label FontSize="20" Foreground="{StaticResource TextLight0}">Jelszó</Label>
                    <Border Style="{StaticResource FocusBorderStyle}" Background="#192633" CornerRadius="5" BorderThickness="1">
                        <PasswordBox Margin="5" Foreground="{StaticResource TextLight1}" Background="Transparent" BorderThickness="0" FontSize="25" x:Name="password"></PasswordBox>
                    </Border>
                    <Border MouseLeftButtonDown="Button_Click" Cursor="Hand" Background="#137fec" Margin="0 20" Padding="0 5" CornerRadius="7">
                        <Label Foreground="{StaticResource Text}" FontWeight="SemiBold" HorizontalAlignment="Center" FontSize="20">Bejelentkezés</Label>
                    </Border>

                </StackPanel>
                <Border Background="#15202b" Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" CornerRadius="0 0 20 20">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
                        <Label FontSize="15" Foreground="{StaticResource TextLight1}">Nincs fiókod?</Label>
                        <Label FontSize="15" Foreground="{StaticResource TextLight0}">Keress fel egy admint!</Label>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

    </Grid>
</UserControl>
